# 複製這個檔案為 .env 後再修改
PORT=4000

# 前端呼叫 API 時需帶的 X-Api-Key，請換成一組長且隨機的字串
API_KEY=change-me-to-a-long-random-secret

# 允許呼叫此 API 的前端來源，多個網址用逗號分隔；內部工具可先用 * 開放全部
ALLOWED_ORIGIN=*

# SQLite 資料庫檔案位置
DB_PATH=./data.db
