# ⚠ 本文件已停用

這份文件原本是「首次建立全新 GCP 環境」用的操作步驟，裡面的資源名稱（例如 `shop-settings-db`、`shop-settings-api`）都只是範例，**跟目前正式環境實際使用的資源名稱不同**。正式環境已經建立好並且在跑（Cloud Run service `body-craft-management-system`），繼續照這份文件操作會建立出重複、衝突的資源。

**請改看 [`docs/gcp-deploy.md`](../docs/gcp-deploy.md)**——那份才是正式環境的唯一基準，記載了實際的資源名稱、標準版更流程、以及「這是既有系統版更、不是首次建立環境」的重要原則。

這份檔案保留在這裡只是為了歷史紀錄（哪些資源名稱曾經被規劃使用過），不再作為操作依據。
